{
  "name": "medverify",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build:client": "cd client && npm run build",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev",
    "inspect:data": "node scripts/inspect-training-data.js",
    "lint:client": "cd client && npm run lint",
    "prepare:rximage": "node scripts/prepare-rximage-dataset.js",
    "train:model": "node scripts/train-visual-model.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "multer": "^2.1.0",
    "sharp": "^0.34.5",
    "uuid": "^13.0.0"
  }
}
